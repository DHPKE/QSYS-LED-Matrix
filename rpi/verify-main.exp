#!/usr/bin/expect -f
set timeout 10
set password "node"
spawn ssh node@10.1.1.22 "grep -A 10 'def on_orientation_change' /home/node/led-matrix/main.py"
expect "password:" { send "${password}\r" }
expect eof
